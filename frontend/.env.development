# ─── Local Development (default) ──────────────────────────────
# Dev mode: uses local mock JSON files, no backend needed
VITE_APP_MODE=dev

# Backend API URL (only used when VITE_APP_MODE=prod)
VITE_API_BASE_URL=http://localhost:8080
