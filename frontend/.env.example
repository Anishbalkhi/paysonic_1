# Paysonic Environment Configuration
# Set to 'dev' for local mock JSON data, or 'prod' to connect to live backend API
REACT_APP_MODE=dev

# Live Backend Base URL (used when REACT_APP_MODE=prod)
REACT_APP_API_BASE_URL=https://api.paysonic.io
